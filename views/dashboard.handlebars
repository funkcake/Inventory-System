<div class="row">
    <div class="col s12 m4 l5">
    </div>

</div>
<div class="row">
    <div style="background-color: #546e7a; position: sticky; top: 0; border-radius: 5px;" class="col s1 catagories">
        <div class="catagoriesList">
            <h1 class="center-align white-text">Inventory Options</h1>
        </div>
        <br>
        <div class="center-align">
            <button class="btn-large waves-effect waves-light" style="background-color:#03a9f4; border-radius: 5px;"
                type="button" id="createData" class="create">Create</button>
        </div>
        {{!-- <br> --}}
        {{!-- <div class="center-align">
            <button class="btn-large waves-effect waves-light" style="background-color:#4caf50;border-radius: 5px;"
                type="button" id="updateData" class="update">Update</button>
        </div>
        <br>
        <div class="center-align">
            <button class="btn-large  waves-effect waves-dark" style="background-color:#d50000; border-radius: 5px;"
                type="button" class="delete">Delete</button>
        </div> --}}
        <br>
        <div class="center-align">
            <button class="btn-large  waves-effect waves-light" type="button"
                style="background-color:blue; border-radius: 5px;" id="logout" class="showAll">Log
                Out</button>
        </div>
        <br>
    </div>

    <div class="col s4 browser-default" style="margin-left:100px">
        <label>
            <h4>Select Category</h4>
        </label>
        <select class="browser-default">
            <option value="" disabled selected>Categories</option>
            <option value="1">PCs</option>
            <option value="2">Monitors</option>
            <option value="3">Laptops</option>
            <option value="4">Servers</option>
            <option value="5">Misc.</option>
            <option value="6">Switches</option>
            <option value="7">Tablets</option>
        </select>
    </div>

    <table class="table striped ">
        <thead>
            <tr>
                <th>TAG#</th>
                <th>Product Name</th>
                <th>Description</th>
                <th>Unit Price</th>
                <th>In Stock</th>
                <th>Category</th>
            </tr>
        </thead>

        <tbody>{{#each products}}
            <tr>
                <td>{{this.dataValues.id}}</td>
                <td>{{this.dataValues.product_name}}</td>
                <td>{{this.dataValues.description}}</td>
                <td>${{this.dataValues.price}}.00</td>
                <td>{{this.dataValues.stock}}</td>
                <td>{{this.dataValues.category.category_name}}</td>
                <td>
                    <a href="#edit=' + {{this.dataValues.id}} + '" data-target="modal1"
                        class="btn-floating waves-effect waves-light orange btn modal-trigger hoverable"><i
                            class="material-icons">edit</i>
                    </a>
                    <a href="#delete=' + {{this.dataValues.id}} + '"
                        class="btn-floating waves-effect waves-light red hoverable"><i class="material-icons">delete</i>
                    </a>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    {{!--
    <!-- Modal Trigger -->
    <a class="waves-effect waves-light btn modal-trigger" data-target="#myModal{{@index}}">Modal</a>

    <!-- Modal Structure -->
    <div id="modal1" class="modal" id="myModal{{@index}}">
        <div class="modal-content">
            <h4>Modal Header</h4>
            <p>A bunch of text</p>
        </div>
        <div class="modal-footer">
            <a href="#!" class="modal-close waves-effect waves-green btn-flat">Agree</a>
        </div>
    </div> --}}

    <!-- Modal Structure -->
    <div id="modal1" class="modal modal-fixed-footer">
        <div class="modal-content">
            <h4>Edit Record</h4>
            <div class="row">
                <form class="col s12">
                    <div class="row">
                        <div class="input-field col s6">
                            <i class="material-icons prefix">account_circle</i>
                            <input placeholder="Placeholder" id="name" type="text" class="validate">
                            <label for="name">Name</label>
                        </div>
                    </div>
                    <div class="row">
                        <div class="input-field col s12">
                            <i class="material-icons prefix">phone</i>
                            <input id="phone" placeholder="Placeholder" type="tel" class="validate">
                            <label for="phone">Phone</label>
                        </div>
                    </div>
                    <div class="row">
                        <div class="input-field col s12">
                            <i class="material-icons prefix">signal_cellular_4_bar</i>
                            <input id="dtmf" placeholder="Placeholder" type="number" class="validate">
                            <label for="dtmf">DTFM</label>
                        </div>
                    </div>
                </form>
            </div>
        </div>
        <div class="modal-footer">
            <a href="#!" class="modal-action modal-close waves-effect waves-green btn-flat ">Update</a>
        </div>
    </div>

    {{!-- <div class="col s9">
        <table style="margin-left:100px;margin-right:auto;">
            <thead style="font-size:15px">
                <tr>
                    <th></th>
                    <th>TAG#</th>
                    <th>Name:</th>
                    <th>Description:</th>
                    <th>Unit Price:</th>
                    <th>Stock</th>
                    <th>Category</th>
                </tr>
            </thead>

            <tbody id="accept">
                {{#each products}}
                <tr>
                    <td>
                        <label>
                            <input type="checkbox" class="checkbox" value="checked" id={{this.dataValues.id}} />
                            <span></span>
                        </label>
                    </td>
                    <td>{{this.dataValues.id}}</td>
                    <td>{{this.dataValues.product_name}}</td>
                    <td>{{this.dataValues.description}}</td>
                    <td>${{this.dataValues.price}}.00</td>
                    <td>{{this.dataValues.stock}}</td>
                    <td>{{this.dataValues.category.category_name}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div> --}}
    {{!--
</div> --}}
</div>



<script src="/javascript/dashboard.js"></script>
<script src="/javascript/logout.js"></script>